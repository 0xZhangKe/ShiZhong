<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhangke.shizhong">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:name=".common.SZApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".page.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page.poster.inputname.InputNameActivity" />
        <activity android:name=".page.poster.inputname.SearchResultActivity"/>
        <activity android:name=".page.poster.showposter.ShowMusicPosterActivity"/>
        <activity android:name=".page.poster.showposter.ShowMoviePosterActivity"/>
        <activity android:name=".page.poster.showposter.ShowMusicAlbumActivity"/>
    </application>

</manifest>